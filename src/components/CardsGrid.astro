---
---

<div class="cards-grid">
  <slot />
</div>

<style>
  .cards-grid {
    display: flex;
    /* grid-template-columns: 1fr 1fr 1fr; */
    flex-direction: column;
    /* align-items: center; */
    /* justify-content: center; */
    gap: var(--spacing);
  }

  .cards-grid > * {
    flex-basis: var(--size-read);
  }
</style>
